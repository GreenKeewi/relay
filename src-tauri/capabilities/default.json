{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for Relay's local widget and dock windows.",
  "windows": ["main", "dock"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
